window.PaiApi={token:localStorage.getItem('paiShoToken'),async request(path,options={}){const response=await fetch(window.PAI_SHO_CONFIG.API_URL+path,{...options,headers:{'Content-Type':'application/json',...(this.token?{Authorization:`Bearer ${this.token}`}:{}) ,...options.headers}});const data=await response.json();if(!response.ok)throw Error(data.error||'Помилка сервера');return data},async login(username,password){const data=await this.request('/auth/login',{method:'POST',body:JSON.stringify({username,password})});this.token=data.token;localStorage.setItem('paiShoToken',data.token);return data.player},logout(){const token=this.token;this.token=null;localStorage.removeItem('paiShoToken');return fetch(window.PAI_SHO_CONFIG.API_URL+'/auth/logout',{method:'POST',headers:{Authorization:`Bearer ${token}`}})}};
